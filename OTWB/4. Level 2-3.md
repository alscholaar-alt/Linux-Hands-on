## Level 2-3
***Aim:*** Find the password from a file named --spaces in this filename-- located in the home directory

***Action taken:*** 
- ran the `ls` command and saw that the `--spaces in this filename--` file was present
- ran the `cat "--spaces in this filename--"` as the quouation marks are needed for files directories with spaces in their names
- another way to access the file would be to use `/` 'cat --spaces/in/this/filename--"
- after realisiing that the the file began with a `-`, I ran the `cat ./"--spaces in this filename--"' command to read the file
- 
